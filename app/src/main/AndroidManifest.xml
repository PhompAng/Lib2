<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phompang.lib2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".StaffActivity" />
        <activity android:name=".AddStaffActivity" />
        <activity android:name=".MemberActivity" />
        <activity android:name=".AddMemberActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".BorrowActivity" />
        <activity android:name=".AddBorrowActivity" />
        <activity android:name=".BorrowReportActivity"></activity>
    </application>

</manifest>
